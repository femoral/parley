/** Layer 3 effect — the PARLEY! ribbon (design-manifest §4.8): a red gradient
 * banner with gold edges, rotated and bobbing over an awaiting island. The one
 * word the operator can't miss from any zoom. Bob is a compositor keyframe. */
export function ParleyRibbon() {
  return (
    <span className="pc-parley" aria-hidden="true">
      PARLEY!
    </span>
  );
}
