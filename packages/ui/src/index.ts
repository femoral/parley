/**
 * `@useparley/ui` — the parley web UI (React + Vite), served by the daemon when
 * installed. This is a private placeholder that completes the four-package
 * workspace graph; the actual UI (serving, discovery, components) lands with
 * ticket #51. See docs/spec/monorepo-layout.md.
 */
export const UI_PLACEHOLDER = true;
