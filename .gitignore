node_modules/
dist/
coverage/

# @useparley/ui build output — the daemon serves it from the `parley.ui` marker
# dir, but it is generated by `vite build` (regenerated on demand for tests via
# scripts/vitest-global-setup.ts), never committed.
packages/ui/www/
*.log
*.db
*.db-shm
*.db-wal
.DS_Store

# TypeScript incremental build metadata
*.tsbuildinfo
