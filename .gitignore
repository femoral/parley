node_modules/
dist/
coverage/

# @useparley/ui and @useparley/dashboard build output — the daemon serves them
# from the `parley.ui` marker dir, but they are generated by `vite build`
# (regenerated on demand for tests via scripts/vitest-global-setup.ts), never
# committed.
packages/ui/www/
packages/dashboard/www/
*.log
*.db
*.db-shm
*.db-wal
.DS_Store

# TypeScript incremental build metadata
*.tsbuildinfo

# Local tool state (impeccable plugin cache, local claude settings)
.impeccable/
.claude/settings.json
